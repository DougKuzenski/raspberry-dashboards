// All instants are stored in UTC and rendered in this zone unless the payload
// specifies another via `timezone`.
export const DEFAULT_TIMEZONE = 'America/Los_Angeles';

// How many days ahead the week agenda looks.
export const AGENDA_DAYS = 7;

// Fixed design canvas; the whole board scales to fit the screen (kiosk pattern).
export const BASE_W = 1280;
export const BASE_H = 720;
